<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.correction.backend.modules.cases.mapper.CollegiateBenchGroupMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.correction.backend.modules.cases.entity.CollegiateBenchGroup">
        <id column="id" property="id" />
        <result column="correction_unit" property="correctionUnit" />
        <result column="user_name" property="userName" />
        <result column="user_id" property="userId" />
        <result column="user_type" property="userType" />
        <result column="sex" property="sex" />
        <result column="id_card" property="idCard" />
        <result column="job" property="job" />
        <result column="data_id" property="dataId" />
        <result column="type" property="type" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, correction_unit, user_name, user_id, user_type, sex, id_card, job, data_id, type
    </sql>

    <!-- 通用查询结果列 别名 -->
    <sql id="Base_Column_List_Alias">
        collegiateBenchGroup.id,collegiateBenchGroup.correction_unit AS correctionUnit,collegiateBenchGroup.user_name AS userName,collegiateBenchGroup.user_id AS userId,collegiateBenchGroup.user_type AS userType,collegiateBenchGroup.sex,collegiateBenchGroup.id_card AS idCard,collegiateBenchGroup.job,collegiateBenchGroup.data_id AS dataId,collegiateBenchGroup.type
    </sql>
</mapper>
